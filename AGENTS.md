# KickMate 저장소 작업 규칙

이 파일은 저장소 전체에 적용되는 변하지 않는 작업 원칙을 정의한다. 하위 디렉터리에 더 구체적인 `AGENTS.md`가 있으면 해당 디렉터리에서는 하위 문서의 규칙을 우선한다.

## 프로젝트 방향

- KickMate는 교대 턴제 축구 보드게임이며, 하나의 순수 엔진을 브라우저 대국·분석·퍼즐·헤드리스 셀프플레이에서 함께 사용한다.
- 작업은 사용자가 지정한 TASK 문서와 완료 기준을 우선한다. 요청 범위를 벗어난 기능 추가나 리팩터링은 하지 않는다.
- 현재 진행 상황, 임시 계획, 마감 일정은 이 파일에 기록하지 않는다. 이 파일에는 장기간 유지할 규칙만 둔다.

## 디렉터리와 책임

- `src/engine/`: 룰, 상태 전이, 평가, 탐색을 구현하는 순수 TypeScript 모듈이다.
- `src/worker/`: 클라이언트와 엔진 사이의 Web Worker 메시지 어댑터만 담당한다.
- `src/client/`: 렌더링, 입력, 대국 흐름 등 브라우저 전용 코드를 둔다.
- `content/`: 퍼즐, 문구, 테마, 밸런스 수치처럼 코드와 분리된 콘텐츠를 둔다.
- `tools/`: 검증기와 셀프플레이 등 개발·운영 도구를 둔다.
- `reference/`: 검증된 프로토타입과 작업 명세를 보관하는 참고 자료다. 명시적인 요청이 없으면 수정, 분리, 정리하거나 런타임 의존성으로 만들지 않는다.

## 엔진 불변 조건

- `src/engine/`에서는 DOM, Web Worker 전역, 파일시스템 및 브라우저 전용 API에 접근하지 않는다.
- 동일한 상태와 옵션은 실행 시각이나 환경과 무관하게 동일한 수와 점수를 반환해야 한다. 엔진 로직에서 `Math.random()`을 사용하지 않는다.
- 상태 전이 함수는 입력 상태를 변경하지 않고 새로운 상태를 반환한다.
- 게임 규칙과 평가 가중치는 TASK 문서 및 `reference/prototype.html`의 검증된 동작을 기준으로 한다.
- 프로토타입과 의도적으로 다른 동작을 도입하려면 이유를 먼저 명시하고, 그 차이를 검증하는 테스트를 함께 추가한다.
- 총 수 제한, 킥오프, 소유권, 득점, 선방 및 스틸 보호 같은 상태 전이는 경계값 테스트로 보호한다.

## 변경 원칙

- 작업 전 `git status`와 관련 파일을 확인하고 기존 미커밋 변경을 보존한다.
- 기능 및 버그 수정은 관찰 가능한 동작을 검증하는 테스트를 먼저 추가하고, 실패 원인을 확인한 뒤 구현한다.
- 기존 구조와 공개 타입을 우선 재사용한다. 새 의존성이나 범위가 큰 추상화는 명확한 필요가 있을 때만 추가한다.
- 사용자가 요청하지 않은 `content/`, `src/client/`, 참고 자료는 컴파일이나 계약 유지에 꼭 필요한 경우가 아니면 수정하지 않는다.
- 커밋, 푸시, 브랜치 생성 및 PR 생성은 사용자가 명시적으로 요청했을 때만 수행한다.

## 주석 원칙

- 주석은 코드가 무엇을 하는지 반복하지 말고, 왜 필요한지와 어떤 불변 조건을 지키는지 설명한다.
- 룰 상수, 평가 가중치, 예외 처리에는 값의 근거나 기준이 되는 TASK·프로토타입 동작을 알 수 있게 적는다.
- 임시 우회에는 필요한 이유, 적용 환경, 제거할 수 있는 조건을 기록한다.
- 코드 변경으로 사실과 달라진 주석은 같은 변경에서 수정하거나 제거한다.
- `TODO`에는 구체적인 후속 작업, 단계 또는 추적 가능한 근거를 포함한다. 의미 없는 자리표시자는 남기지 않는다.
- 공개 타입과 모듈 경계에는 책임과 제약을 짧은 문서 주석으로 설명한다.
- 이름과 타입만으로 의도가 분명한 코드에는 주석을 추가하지 않는다.
- 주석과 개발 문서는 기본적으로 한국어로 작성하되, API 이름·표준 용어·코드 식별자는 원문을 유지한다.

## 검증과 완료 기준

- 변경 중에는 가장 좁은 관련 테스트를 먼저 실행하고, 완료 전 저장소 루트에서 `npm run check`를 실행한다.
- `npm run check`의 typecheck, Vitest, content 검증이 모두 성공해야 완료로 보고할 수 있다.
- 테스트를 실행하지 못했거나 일부만 확인했다면 통과했다고 표현하지 말고, 확인한 범위와 차단 원인을 명시한다.
- 완료 전 `git diff --check`, 변경 파일 목록, S1 스텁 및 엔진의 금지 의존성 잔존 여부를 확인한다.
